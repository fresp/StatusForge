- Implemented WebhookEvent struct and DispatchWebhookEvent function for best-effort webhook delivery.
- Used 10-second context timeout for loading channels and 5-second client timeout for HTTP requests.
- Ensured non-fatal delivery with logging of failures and non-2xx responses.- Task 3 update: removed per-channel goroutines so DispatchWebhookEvent now sends synchronously per enabled channel while remaining non-fatal (logs and continues).
- Task 3 update: explicitly drains each HTTP response body with io.Copy(io.Discard, resp.Body) before Close, with drain/close error logging to preserve connection reuse safety.
- Task 6 update: maintenance create/update handlers can safely call DispatchWebhookEvent immediately after successful DB mutation because delivery is best-effort and does not impact CRUD response flow.
- Task 4 learning: admin webhook channel routes were placed directly after subscriber admin routes in api_routes.go to match existing admin CRUD grouping conventions without introducing extra abstractions.
- Task 5 update: incident create/update/update-log handlers now dispatch webhook events immediately after successful DB writes, while preserving existing websocket BroadcastEvent behavior.
- Task 7 learning: AdminWebhookChannels.tsx already existed with full CRUD UI; added WebhookChannel type to types/index.ts following existing type patterns (id, name, url, enabled, createdAt). Page conventions match AdminSubscribers.tsx: useApi hook, confirm() before delete, refetch() after mutations, inline error for create failures, padded container with title+count header, white bordered card table.
- Task 8 learning: Route wiring in App.tsx follows exact AdminSubscribers pattern - import component, add Route with path="webhook-channels" wrapped in RoleRoute allowed=["admin"]. Navigation item added to AdminLayout.tsx navItems array with Webhook icon from lucide-react, placed after Subscribers. Operator visibility unchanged - whitelist approach (OPERATOR_ALLOWED set) automatically excludes webhook-channels since not explicitly allowed.
